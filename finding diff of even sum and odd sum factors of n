#include<stdio.h>
void main()
{
	int n,i=1,odd=0,even=0,sum;
	scanf("%d",&n);
	while(i<=n)
	{
		if(n%i==0)
		{
			if(i%2==0)
			even+=i;
			else
			odd+=i;
	    }
	    i++;
	}
	sum=even-odd;
	if(even==0)
	printf("NO");
	else
	printf("sum is %d",sum);
}
