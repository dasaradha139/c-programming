#include<stdio.h>
void main ()
{
int n,i=1,sum=0;
printf("enter numbers:");
scanf("%d",&n);
while(i<n)
{
if(n%i==0)
sum+=i;
i++;
}
if(sum==n)
printf("%d is perfect number",n);
else
printf("%d is not perfect number",n);
}
