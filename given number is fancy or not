//even count=odd count
#include<stdio.h>
void main()
{
	int n,even=0,odd=0,r;
	scanf("%d",&n);
	while(n>0)
	{
		r=n%10;
		if(r%2==0)
		even++;
		else
		odd++;
		n=n/10;
	}
	if(even==odd)
	printf("FANCY");
	else
	printf("NOT FANCY");
}
