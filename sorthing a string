#include<stdio.h>
#include<string.h>
void main()
{
	char a[30],n,i,j;
	gets(a);
	n=strlen(a);
	for(i=0;i<n;i++)
	{
		for(j=i;j<n;j++)
		if(a[i]>a[j])
		{
		char temp=a[i];
		a[i]=a[j];
		a[j]=temp;
	   }
    }
    puts(a);
}
